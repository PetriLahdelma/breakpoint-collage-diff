Show HN: breakpoint-collage-diff — collage breakpoints and catch visual regressions fast

breakpoint-collage-diff captures breakpoint screenshots with Playwright, stitches a single collage, and diffs against baselines. Quickstart:

```bash
npx breakpoint-collage-diff --url https://example.com
```

Ideal for responsive UI reviews in CI.
