I just shipped breakpoint-collage-diff, a CLI that captures breakpoints with Playwright, stitches a collage, and diffs against baselines. It is great for fast responsive UI reviews.

Quickstart:
```bash
npx breakpoint-collage-diff --url https://example.com
```

If you do visual regression testing, I would love feedback.
