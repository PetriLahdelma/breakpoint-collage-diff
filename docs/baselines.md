# Baselines

Baselines live in `baseline/` by default. Update with:

```bash
breakpoint-collage-diff --url https://example.com --update-baseline
```

Store baselines in git and review changes via PR diffs.
