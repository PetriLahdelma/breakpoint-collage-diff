# Constraints & Limitations

- Requires Playwright browsers (`npx playwright install`).
- Dynamic content must be stabilized (animations, time, randomized data).
- Large pages increase capture time and collage size.

Performance notes: scales with URLs × breakpoints; Playwright capture dominates.
